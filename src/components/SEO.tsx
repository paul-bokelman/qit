import Head from "next/head";

const SEO: React.FC = () => (
  <Head>
    <title>Queue It</title>
    <meta name="description" content="Personalize your party." />
    <link rel="icon" href="/favicon.ico" />
  </Head>
);

export default SEO;
